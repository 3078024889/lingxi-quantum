灵犀 LINGXI · v130（终极排查版：表单输入框、评分按钮、gate页、多维叙事
开篇、四大修炼技术、探索页全部补齐；阅读面调浅）

★ 在 v129 基础上的改动，直接上传这一个 v130 即可（累计包含 v49-v129
全部内容）。这版是照你这几轮反馈做的一次系统性收尾，不是修一两个点。

【一、生命图谱表单：输入框和评分按钮之前是纯黑，这次改了】

表单里"姓名""出生日期""年月日""时分""出生地点"这些输入框，用的是
bg-lm2-bg 这个类——跟首页卡片用的 bg-void 是两套独立的类名，之前我
只顾着改 bg-void，这套一直没跟着换，所以看起来还是黑的。这次把
bg-lm2-bg 也接进了跟输入框同一套的极光蓝玻璃处理。
自评分那几行（能量水平/头脑清晰度/内外对齐感）的评分按钮，未选中
状态用的也是这个黑色底，选中状态是紫色——这次统一成了都用极光玻璃，
选中态保留紫色高亮区分，不再是"一半紫一半黑"的拼接感。

【二、阅读类背景整体调浅了一档】

你反馈"多维叙事阅读器方向是对的，但稍深"——这次把这个阅读专用的
背景（新建了一个统一的 .bg-reading-glass 类）从原来偏深的版本调浅了
一档，同时把这一个类应用到了所有"长文阅读"场景，不再是各处分别用
不同深浅的底色：
- 多维叙事阅读器（含未解锁时的"开启完整传输"提示区，之前这块跟着
  外层一起没有框，这次一并处理了）
- 四大修炼技术详情页（呼吸法/直觉丹道/上升心经/归零心诀）的全部正文
- 探索页文章详情（含 manifestation-methods 这类走公共组件的文章，
  也包括 learn/dream、learn/wingmakers、learn/manifestation 这三篇
  没走公共组件、单独写的文章——这三篇之前漏改了，这次一起找出来改了）
- 探索页文章底部"延伸：xxx · xxx"链接行，之前完全没有框
- 术语表（/glossary）整页
- 系统声明（/declaration）整页

【三、重塑潜意识（gate 详情页）】

"你诞生于源……"这段介绍文字、"九个邀请"整块，之前都是裸文字，这次
包进了阅读面玻璃。

【四、其他之前漏掉的整页】

/membership（能量交换）首屏、/narrative（多维叙事列表页）首屏、
/account 的"欢迎回来"和"连接到你的意识场"两个状态，之前都是裸文字，
这次都补上了。

【验证方式】

改完直接查了浏览器算出来的样式：declaration、glossary、gate/origin、
learn/dream（单独写的那篇）这几处，背景色确认都是统一的
rgba(14,24,42,0.34)（阅读面玻璃，同一个值，不是分别调的）；输入框的
CSS 规则直接在编译产物里核对了，input.bg-lm2-bg 和 input.bg-void 现在
共用同一条规则。
