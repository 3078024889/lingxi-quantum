<view class="lx-page">
  <view class="lx-orbit"></view>
  <field-nav showBack="{{true}}" title="{{lang === 'en' ? 'Digital Service' : '数字服务'}}"/>
  <view wx:if="{{loading}}" class="lx-loading detail-state">{{lang === 'en' ? 'Loading service…' : '正在展开内容…'}}</view>
  <view wx:elif="{{loadError}}" class="lx-content detail-state">
    <view class="lx-empty lx-veil"><view>{{loadError}}</view><button class="lx-button compact" bindtap="loadItem">重新连接</button></view>
  </view>
  <view wx:elif="{{item}}" class="lx-content">
    <view class="detail lx-veil">
      <view class="lx-kicker">LINGXIFIELD DIGITAL SERVICE</view>
      <view class="lx-title">{{lang === 'en' ? item.nameEn : item.name}}</view>
      <view class="lx-subtitle">{{lang === 'en' ? item.noteEn : item.note}}</view>
      <view wx:if="{{item.detailDescription}}" class="detail-description">{{lang === 'en' ? item.detailDescriptionEn : item.detailDescription}}</view>
      <view wx:if="{{item.benefits.length}}" class="detail-benefits"><view wx:for="{{item.benefits}}" wx:key="title" class="detail-benefit"><view class="detail-benefit-title">{{lang === 'en' ? item.titleEn : item.title}}</view><view wx:if="{{item.detail}}" class="detail-benefit-note">{{lang === 'en' ? item.detailEn : item.detail}}</view></view></view>
      <view wx:if="{{item.closing}}" class="detail-closing">{{item.closing}}</view>
      <view wx:if="{{item.closingEn}}" class="detail-copy-en">{{item.closingEn}}</view>
      <view class="promise" wx:if="{{item.category === 'report'}}"><view>结构识别<text>STRUCTURE</text></view><view>现实映射<text>REALITY</text></view><view>反证条件<text>FALSIFIER</text></view><view>行动实验<text>EXPERIMENT</text></view></view>
      <view class="narrative-promise" wx:if="{{item.category === 'narrative'}}">先不要急着寻找答案。<br/>静静感受：这段叙事，是否映照了你正在经历，却尚未完全表达的生命状态。</view>
      <view class="narrative-promise" wx:elif="{{item.category === 'practice'}}">不是观看一套课程，而是把一项可重复的修炼带回身体与日常。</view>
      <view class="narrative-promise" wx:elif="{{item.category === 'membership'}}">在有效期内进入对应场域；神尊全域解锁同时包含未来新增的报告、叙事、修炼技术与场域模块。</view>
      <view wx:if="{{owned}}" class="owned-note">{{lang === 'en' ? 'Available in My Field ✓' : '已在你的场域中 ✓'}}</view>
      <view wx:if="{{!owned}}" class="lx-price price">¥ {{item.priceFen / 100}}</view>
      <view wx:if="{{!owned}}" class="purchase-disclosure">
        <view class="disclosure-row"><view class="disclosure-label">交付方式</view><view class="disclosure-value">{{deliveryLabel}}</view></view>
        <view class="disclosure-row"><view class="disclosure-label">权益期限</view><view class="disclosure-value">{{validityLabel}}</view></view>
        <view class="disclosure-row"><view class="disclosure-label">续费方式</view><view class="disclosure-value">不会自动续费，到期后由你决定是否继续</view></view>
      </view>
      <view wx:if="{{!owned}}" class="agreement">
        <checkbox-group bindchange="onAgreementChange">
          <label class="agreement-check"><checkbox value="confirmed" checked="{{agreed}}" color="#76ded2"/><text>我已阅读并同意</text></label>
        </checkbox-group>
        <view class="agreement-links">
          <text class="policy-link" data-path="/terms" bindtap="openPolicy">《服务条款》</text>
          <text class="policy-link" data-path="/refunds" bindtap="openPolicy">《退款政策》</text>
          <text class="policy-link" data-path="/privacy" bindtap="openPolicy">《隐私政策》</text>
        </view>
      </view>
      <button wx:if="{{owned}}" class="lx-button" disabled="{{opening}}" loading="{{opening}}" bindtap="openOwned">{{lang === 'en' ? 'OPEN SERVICE' : '打开已开启内容'}}</button>
      <button wx:else class="lx-button" disabled="{{paying || !agreed}}" loading="{{paying}}" bindtap="pay">{{lang === 'en' ? (item.ctaEn || 'CONTINUE TO PAYMENT') : (item.cta || '继续支付')}}</button>
      <view class="fineprint">{{owned ? '阅读权限会由服务端再次核验，确保内容只向已开启的账户开放。' : '虚拟内容将在支付确认后自动交付。最终权益以服务端支付通知为准。'}}</view>
      <button class="support-button" open-type="contact">付款或权益需要协助？联系微信客服</button>
    </view>
  </view>
</view>
