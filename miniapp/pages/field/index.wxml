<view class="lx-page"><view class="lx-orbit"></view><field-nav /><view class="lx-content">
  <view class="hero"><view class="lx-kicker">A Living Digital Field</view><view class="lx-title">不是浏览一个应用，<br/>而是步入一座回应你的意识场域。</view><view class="lx-subtitle">意识观测、梦境碎片、生命线索、修炼技术与多维叙事在此交汇，成为一面照见你内在的真实镜像。</view></view>
  <view class="lx-grid lx-section"><view wx:for="{{entries}}" wx:key="title" class="lx-card lx-veil" data-index="{{index}}" bindtap="enter"><view class="lx-card-title">{{item.title}}</view><view class="lx-card-note">{{item.note}}</view><view class="arrow">进入 →</view></view></view>
  <button class="website-entry" bindtap="openWebsite">进入灵犀场官网 →</button>
  <view wx:if="{{exchanges.length}}" class="lx-section"><view class="lx-kicker">PRACTICE &amp; ACCESS</view><view class="lx-section-title exchange-title">修炼与全域解锁</view><view class="lx-subtitle">从一次观测，到持续探索。<br/>灵犀场记录你的探索轨迹，并持续开放新的场域内容。</view><view class="exchange-list"><view wx:for="{{exchanges}}" wx:key="productId" class="exchange lx-veil {{expandedProductId === item.productId ? 'expanded' : ''}}"><view class="exchange-summary" data-product="{{item.productId}}" bindtap="toggleExchange"><view class="exchange-heading"><view class="lx-card-title">{{item.name}}</view><view class="lx-card-note">{{item.note}}</view></view><view class="exchange-price">{{item.priceLabel}} {{expandedProductId === item.productId ? '⌃' : '⌄'}}</view></view><view wx:if="{{expandedProductId === item.productId}}" class="exchange-detail"><view class="exchange-description">{{item.detailDescription}}</view><view class="benefit-list"><view wx:for="{{item.benefits}}" wx:for-item="benefit" wx:key="title" class="benefit-item"><view class="benefit-title">{{benefit.title}}</view><view wx:if="{{benefit.detail}}" class="benefit-detail">{{benefit.detail}}</view></view></view><view wx:if="{{item.closing}}" class="exchange-closing">{{item.closing}}</view><button class="lx-button exchange-action" data-index="{{index}}" catchtap="openExchange">{{item.cta}}</button></view></view></view></view>
  <view class="lx-veil manifesto"><view class="lx-kicker">LINGXI FIELD</view><view class="lx-section-title">此刻，就是入口</view><view class="lx-subtitle">灵犀场不替你决定方向。<br/><br/>因为真正的答案，<br/>一直存在于你与自己的连接之中。<br/><br/>这里提供观察的镜面、<br/>理解生命结构的路径，<br/>以及重新看见自己的空间。<br/><br/>每一次进入，<br/>都是一次与自身更深层连接的开始。</view></view>
</view></view>
