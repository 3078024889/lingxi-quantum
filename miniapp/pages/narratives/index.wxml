<view class="lx-page"><field-nav/><view class="lx-content"><view class="lx-kicker">MULTIDIMENSIONAL NARRATIVES</view><view class="lx-title">多维叙事</view><view class="lx-subtitle">先不要急着寻找答案。<br/>静静感受：<br/>这段叙事，是否映照了你正在经历，<br/>却尚未完全表达的生命状态。<text class="page-en">Do not rush toward an answer. Notice whether this story reflects a life-state you are living but have not fully named.</text></view><view wx:if="{{loading}}" class="lx-loading">正在展开叙事… · OPENING STORIES</view><view wx:else class="lx-section"><view wx:for="{{items}}" wx:key="productId" class="lx-card lx-veil narrative" data-sku="{{item.skuId}}" data-product="{{item.productId}}" bindtap="open"><view class="story-label">{{item.owned ? '已在你的场域中 · IN YOUR FIELD' : item.skuId === 'sub_narrative_365' ? '年度叙事通行证 · ANNUAL PASS' : '读前一问 · BEFORE READING'}}</view><view class="lx-card-title">{{item.name}}</view><view wx:if="{{item.nameEn}}" class="card-en">{{item.nameEn}}</view><view class="lx-card-note">{{item.note}}</view><view wx:if="{{item.noteEn}}" class="card-note-en">{{item.noteEn}}</view><view class="story-open">{{item.owned ? '阅读全文 · READ →' : '读完整入口 · FULL STORY · ¥ ' + (item.priceFen / 100) + ' →'}}</view></view></view></view></view>
