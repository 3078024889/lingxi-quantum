# 灵犀场 LINGXIFIELD · 微信小程序

当前小程序是灵犀场主站的移动入口，不再使用早期“场域 / 精测 / 叙事”架构。

## 当前原生入口

- **SASI 创作**：进入创作、短剧、网站构建与资料知识能力。
- **实用工具**：进入 PDF、图片、视频、文字、隐私与文件处理工具。
- **资料智库**：进入书本、论文、学习与科研资料空间。
- **我的**：账户、订单、创作余额、账号连接、语言与客服。
- **WebView**：只允许经过白名单校验的灵犀场站内页面。

## 语言

当前界面语言结构：

- 中文
- English
- 日本語
- 한국어
- Français
- Deutsch
- Español
- Português
- العربية

所有用户可见文字以功能和结果为中心。后台实现、模型厂商、任务节点、API、运行机制不作为普通用户界面说明。

## 身份

- 使用 `wx.login` 建立微信小程序身份。
- 客户端只保存短期不透明会话信息。
- AppSecret、服务端密钥、支付签名密钥不得进入小程序包。
- 已有灵犀场网页账户只有在用户主动确认后才连接；禁止按邮箱、手机号、昵称或历史订单自动猜测合并。

## 页面安全边界

- API 客户端只允许访问 `/api/wechat/mini/` 路径。
- WebView 只允许显式白名单中的站内路径。
- 拒绝外部 scheme、协议相对 URL、路径穿越和未知入口。
- 账户/订单数据不会通过分享入口发送。

## 支付

早期原生 `miniapp/utils/payment.js` 已退出当前客户端架构，不应为了兼容历史审计重新恢复。

服务端仍可保留微信小程序支付相关接口，用于后续经真实产品入口验证后的支付闭环。支付能力必须满足：

- 用户身份验证
- 金额与商品服务端确定
- 订单归属校验
- 回调签名验证
- 幂等发货
- Rate Limit
- 沙箱与生产密钥隔离

没有真实用户入口调用的旧客户端支付代码不得作为上线必需项。

## 发布前验收

必须通过：

- TypeScript / Build
- SASI audit
- Mini Program audit
- Security audit
- diff check
- 微信开发者工具真机路径验证

生产数据库变更、Git commit/push、小程序上传与提交审核均独立执行，不由普通代码补丁隐式触发。
