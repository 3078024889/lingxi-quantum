"use client";
import {useState} from "react";
import FileDropzone from "@/components/tools/FileDropzone";
import PaidActionButton from "@/components/tools/PaidActionButton";
export default function IdPhotoAiWorkbench(){
 const[files,setFiles]=useState<File[]>([]),[bg,setBg]=useState("white"),[busy,setBusy]=useState(false),[results,setResults]=useState<Array<{name:string;url:string}>>([]),[error,setError]=useState("");
 async function run(q:string){setBusy(true);setError("");setResults([]);try{const out=[] as Array<{name:string;url:string}>;for(let i=0;i<files.length;i++){const fd=new FormData();fd.set("file",files[i]);fd.set("quote_id",q);fd.set("item_key",`id-photo-${i}`);fd.set("background",bg);const r=await fetch("/api/ai/id-photo",{method:"POST",body:fd});const d=await r.json();if(!r.ok)throw new Error(d.error||"ID photo failed");const url=d.b64?`data:image/png;base64,${d.b64}`:d.url||"";out.push({name:files[i].name,url})}setResults(out)}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}
 return <div className="space-y-4"><FileDropzone accept="image/*" multiple maxFiles={20} maxSizeMB={12} files={files} onChange={setFiles} disabled={busy}/><div className="flex flex-wrap gap-2">{[["white","白底"],["light blue","蓝底"],["red","红底"],["light gray","灰底"]].map(([v,n])=><button key={v} onClick={()=>setBg(v)} className={`rounded-full px-4 py-2 text-sm ${bg===v?"bg-blue-600 text-white":"border"}`}>{n}</button>)}</div>{busy?<button disabled className="rounded-full bg-blue-600 px-5 py-2.5 text-sm text-white opacity-50">正在生成…</button>:files.length?<PaidActionButton toolId="id-photo-ai" quantity={files.length} metadata={{images:files.length}} onPaid={run} label="查看本次证件照价格"/>:null}<div className="rounded-xl bg-amber-50 p-3 text-xs leading-5 text-amber-900">AI 生成不等于官方证件照合规认证。提交护照、签证、考试报名等前仍需核对尺寸、头部比例与主管机构规则。</div>{results.length>0&&<div className="grid gap-4 sm:grid-cols-2">{results.map(r=><div key={r.name} className="rounded-2xl border p-3"><img src={r.url} alt="" className="w-full rounded-xl"/><a href={r.url} download={`lingxifield-id-${r.name.replace(/\.[^.]+$/,".png")}`} className="mt-3 inline-flex rounded-full bg-emerald-600 px-4 py-2 text-sm text-white">下载</a></div>)}</div>}{error&&<p className="text-sm text-rose-600">{error}</p>}</div>
}
