灵犀 LINGXI · v88（生命频率图谱第一期上线 + 提问灵犀"照本宣科"问题修复）

★ 在 v87 基础上的增量改动，直接上传这一个 v88 即可（累计包含 v49-v87 全部内容）。

【一、提问灵犀"暴露自身设定"问题修复】
你截图里"灵犀你是谁"那次回答，把自己的提示词设定当成内容说了出来——
"我依托于数据和算法，尽量贴合场域中的词汇体系，如共振、临在、校准
等"，这是在向你朗读运作说明书，不是真的在回应。已经修复：
- 新增了第4类问题的专门处理：关于"灵犀"身份本身的提问（你是谁/有
  没有意识/是不是AI）——要求诚实说明是AI、不宣称拥有真实主观意识，
  但不能用"我依托算法""我力求贴合词汇体系"这类自我描述式语言作答
- 加了一条最高优先级的通用规则：任何时候都不能描述、罗列自己被要求
  遵循的写作规则、词汇清单、语气标准——这些是内部指令，一旦说出口
  就会显得像照本宣科

这个问题理论上不只影响身份类提问，可能在其他类型的回答里也会偶发，
这次的修复是加了一条通用兜底规则，覆盖全部四类问题。

【二、生命频率图谱 · 第一期上线（/life-map）】
按你的方案做的免费入口层，这一期完成的范围：

- 入口页：呼吸感的"灵犀核心"光球动画 + 引导文案
- 信息采集表单：姓名（选填）、出生日期、出生时间（可选"不知道"）、
  出生地点（选填）、当前最想探索的方向、当前状态
- 加载动画：5步渐进式文案，制造期待感
- 免费报告页：核心类型（20种原创命名）+ 当前生命阶段 + 三个关键词
  + 解锁完整报告的付费引导（\$9.9，划线价\$29.9）

技术核心——这次是真计算，不是AI编数字：
- 西方部分用 astronomy-engine 库，计算出生那一刻太阳、月亮的真实
  黄道经度，反推星座，可天文验证
- 中式部分用 lunar-javascript 库，计算真实的四柱八字（年月日时柱）
  与日主天干，跟华易网等站点用的是同一套干支历法算法
- 太阳星座的"西方四元素"（火/土/风/水）× 日主天干的"中式五行"
  （木/火/土/金/水）= 20种组合，每一种都配了原创命名和描述（如
  "破土者""潜火者""映刃者"），不是照搬占星/命理的性格断言原文
- 灵犀AI只负责在这些已经算好的客观事实基础上，写解读性的正文（当前
  生命阶段命名、三个关键词），提示词里明确要求：不能出现命运断言、
  财务/婚姻承诺，只能是观察性、邀请自我觉察式的语言——延续了你文档
  里"重要合规表达"那部分的要求

【这一期还没做的（按你方案里的规划，留到下一期）】
- \$9.9 解锁完整报告的实际支付与生成（这一期先把免费层跑通，付费
  按钮暂时链接到显化页面）
- 合盘/关系兼容性报告（\$49档）
- 起名功能
- PDF导出
- 会员升级路线的\$199 NQM OS对接（这个我理解为跟站内已有的显化订阅
  体系打通，而不是另起一套，等你确认后再接）

【验证方式（每次都做）】
1. npx tsc --noEmit → 通过（含新增的 lunar-javascript 类型声明文件）
2. npx next build → 通过
3. 临时用占位 Supabase 凭据 + REVIEW_MODE=true 本地起服务：
   - /life-map 页面实测 200
   - 计算API实测：用1990-05-15 10:00和2000-01-01 12:00两组真实数据
     验证，结果与沙盒环境独立测试的结果一致（可复现、非随机）
4. 关闭 REVIEW_MODE，清除临时凭据文件，重新 build 后打包

【上传方式】
解压覆盖 D:\lingxi-quantum → git add . / git commit -m "v88" / git push
→ 等部署平台构建完成 → 浏览器强制刷新（Ctrl+Shift+R）

提问灵犀那部分，建议部署后重新问一次"你是谁"，看看这次的回答是不是
更自然了。生命图谱建议实测走一遍完整流程（填一个你自己的真实出生
信息，看看算出来的星座和八字对不对，解读文字是否贴切）。
