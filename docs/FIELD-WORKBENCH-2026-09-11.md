# 场域精测工作区：第一批

本批仅上线到代码：生命灵签三栏工作区、生命图谱原内容恢复。其余产品按用户补充的效果图分批处理。

## 内容与行为

- `/qian`：原介绍、填写、FAQ 放到左栏；中栏保留三签揭示、原 11 项完整档案说明、分享入口，并补充每枚签已有的免费释义。保留全部 64 张签图和 WebGL 环形动画；减少动态效果偏好使用真实签库静态网格。
- 灵签支付前确认，支付返回 `/qian?archive=...`；账户灵签记录进入同一工作区，右栏复用原报告组件及其权限检查、PDF 导出。旧 `/qian/full` 仍兼容。
- `/life-map`：从 `8511908^` 恢复七大行星、四柱、五行、玛雅、紫微、吠陀、生命密码、人类图、选填数字能量预览与 4 个 FAQ。恢复原 13 项内容说明，实际付费报告仍为 15 章。
- 介绍全文以展开方式保留，填写入口保持可见。封面图为设计展示，不带虚构用户、日期或解锁状态。

## 验证

- `npm run build`：通过（仓库仍有 lint 警告）。
- `npm run audit:qian`、`npm run audit:lifemap`：通过。
- 灵签原有 47 个中文字符串全部保留；签图数量为 64。
- 浏览器验证：桌面三栏、填写入口、390px 手机端无横向溢出、减少动态效果的签库网格。
- 测试日期 1990-06-15 的生命图谱调用实际计算接口返回 200；七行星、命盘节点、13 项说明出现在付款按钮之前。
- 未完成真实登录后的灵签提交、付款、付费 PDF 下载端到端验证；不宣称已验证生产支付或已部署。

## 后续批次

1. 桃花磁场、今日潮汐、生命韧性、关系共振、财富创造地图、量子生命镜像：按各自参考图使用独立色彩和原内容，不复制示例分数、用户或价格。
2. 生命原型：以真实同主体八流汇聚进度为准，保留生成条件。
3. 潜意识、我的场域与订阅入口：沿用真实状态，按补充参考图分别推进。
4. 修炼技术总览、上升心经、归零心诀、直觉丹道、量子息法：保留原步骤与文案，接真实练习记录。

`.cn` 排查：用户截图为 nginx 502；本次本机 HEAD 请求 `.cn` 与 `.com` 均返回 200，`.cn` 请求约 12 秒，含 Vercel 上游响应头。仅能推断代理/上游链路存在排查必要，不能确认根因；未修改 DNS、服务器或线上入口。
