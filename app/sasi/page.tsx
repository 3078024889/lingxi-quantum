import type {Metadata} from "next";import {redirect} from "next/navigation";import Nav from "@/components/Nav";import Footer from "@/components/Footer";import SasiCommandCenter from "@/components/SasiCommandCenter";import SasiAutonomyEntrances from "@/components/SasiAutonomyEntrances";
export const dynamic="force-dynamic";
export const metadata:Metadata={title:"灵犀场 SASI｜创作、短剧、资料智能体与科研",description:"SASI 是灵犀场的创作与任务入口：自主图片、短剧、资料智能体、科研与可选增强生成，从一个任务进入可执行工作流。",alternates:{canonical:"/sasi"}};
export default function SasiPage({searchParams}:{searchParams?:{view?:string}}){const view=typeof searchParams?.view==="string"?searchParams.view:"";if(view==="billing")redirect("/sasi/pricing");if(view==="drama"||view==="director")redirect("/sasi/drama");if(view==="connections")redirect("/sasi/connections");if(view==="account")redirect("/account");return <><Nav/><SasiCommandCenter/><SasiAutonomyEntrances/><Footer/></>}
