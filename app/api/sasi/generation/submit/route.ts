import {NextRequest,NextResponse} from "next/server";
import {createClient} from "@/lib/supabase/server";
import {isSameOriginMutation} from "@/lib/sasi/request-security";
import {submitWorkerTask} from "@/lib/sasi-kernel/worker/client";
export const runtime="nodejs";export const maxDuration=30;
export async function POST(req:NextRequest){if(!isSameOriginMutation(req))return NextResponse.json({error:"INVALID_REQUEST_ORIGIN"},{status:403});const supabase=createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});const body=await req.json().catch(()=>({}));const kind=body.kind==="video"?"video":body.kind==="image"?"image":null,prompt=String(body.prompt||"").trim();if(!kind||prompt.length<2||prompt.length>4000)return NextResponse.json({error:"INVALID_GENERATION_REQUEST"},{status:400});const taskId=crypto.randomUUID(),nodeId="generate";try{const result=await submitWorkerTask({requestId:crypto.randomUUID(),taskId,nodeId,capabilityId:kind==="image"?"image.generate.diffusion":"video.generate.model",input:{prompt,ratio:["1:1","16:9","9:16"].includes(String(body.ratio))?String(body.ratio):"1:1",duration:kind==="video"?Math.max(2,Math.min(30,Number(body.duration)||6)):undefined,style:String(body.style||"")}});return NextResponse.json({ok:result.status==="succeeded",result},{status:result.status==="succeeded"?200:503,headers:{"Cache-Control":"no-store"}});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"GENERATION_UNAVAILABLE"},{status:503});}}
