export type SasiProvenanceDecision={allowIngest:boolean;allowLearning:boolean;reason:"owned-or-authorized"|"public-metadata"|"compatible-open-license"|"license-unknown-reference-only"|"reference-only"};export function decideSasiProvenance(input:{sourceType:string;licenseName?:string|null;ownedOrAuthorized?:boolean}):SasiProvenanceDecision{if(input.ownedOrAuthorized)return{allowIngest:true,allowLearning:true,reason:"owned-or-authorized"};if(input.sourceType==="public-metadata")return{allowIngest:true,allowLearning:true,reason:"public-metadata"};const license=input.licenseName?.trim().toLowerCase();if(!license)return{allowIngest:true,allowLearning:false,reason:"license-unknown-reference-only"};if(["cc0","public domain","cc by","apache","mit","bsd"].some(x=>license.includes(x)))return{allowIngest:true,allowLearning:true,reason:"compatible-open-license"};return{allowIngest:true,allowLearning:false,reason:"reference-only"}}
