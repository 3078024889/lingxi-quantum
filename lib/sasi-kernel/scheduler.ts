import type {SasiCapabilityManifest,SasiRuntimeTarget} from "./types";
export type SchedulerSnapshot={browserAvailable:boolean;workerReady:boolean;serverAvailable:boolean;externalAllowed:boolean;availableRamMb:number;gpuAvailable:boolean};
export function chooseRuntimeTarget(manifest:SasiCapabilityManifest,s:SchedulerSnapshot):SasiRuntimeTarget|null{const targets=manifest.runtimeTargets;const allowed=(target:SasiRuntimeTarget)=>target==="browser"?s.browserAvailable:target==="server"?s.serverAvailable:target==="worker"?s.workerReady&&(manifest.resources.gpu!=="required"||s.gpuAvailable):target==="external"?s.externalAllowed:false;const order:SasiRuntimeTarget[]=manifest.localFirst?["browser","server","worker","external"]:["worker","server","browser","external"];for(const target of order)if(targets.includes(target)&&allowed(target))return target;return null}
