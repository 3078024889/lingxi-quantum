import{capabilityManifests,executableCapabilities}from"./registry";
export function sasiKernelReadiness(){const manifests=capabilityManifests(),executable=executableCapabilities();return{kernelVersion:"18.0",platform:true,singleRegistry:true,capabilityCount:manifests.length,executableCapabilityCount:executable.length,localFirstCount:manifests.filter(x=>x.localFirst).length,externalOptionalCount:manifests.filter(x=>x.externalOptional).length,durableTaskSchema:true,artifactSchema:true,computeWorkerProtocol:true,taskStateMachine:true,scheduler:true,recoveryPolicy:true,artifactValidation:true,legacyAutonomousFolderRequired:false};}
