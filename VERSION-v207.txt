灵犀 LINGXI · v207（两份报告加上封面页——LOGO+标题+已揭示的三张
牌/三枚签+日期，不需要另外设计专门的封面插画，你说得对，直接用
已有素材拼起来就够了；已用 npx tsc --noEmit 和 npm run build 双重
验证，0 类型错误、生产构建成功）

★ 在 v206 基础上的改动，直接上传这一个 v207 即可（累计包含此前全部内容）。
★★ 这次不需要SQL。

【封面页——按你说的最简单方式做的】
app/qian/full/QianReport.tsx、app/tarot/reading/full/
TarotReadingReport.tsx
- 结果页开头，现在是一个独立的封面区块：LOGO（用的是你发的那张
  真实图片）、报告英文品牌名（LINGXI LIFE ORACLE / LINGXI QUANTUM
  TAROT）、报告标题、副标题、已经揭示的三张牌/三枚签的图片（原本
  就在页面里，这次挪到封面区块里当封面视觉用）、称呼+日期、网址——
  没有另外设计一张专门的插画，就是你说的"翻出来的卡牌配上LOGO"
  这个思路，这个区块本身也是PDF导出时的第一个独立章节，下载出来
  的PDF第一页看起来就是一张完整的封面。
- 报告标题同步升级成"生命原型档案""生命镜像档案"这个更正式的
  叫法（上次已经改过，这次封面延续用的是同一套）。

【关于文档提到的"多语言自动切换"——按你说的确认了，不需要】
你说清楚了，只需要中英文双语，站内本来就是这套（用户可以切换
简体中文/English），不用额外做日语、西语、法语这些，之前v206
说"这个没做"，其实不是漏了，是不需要做，这次确认清楚。

【Airwallex/PayPal那边——纯提醒，不涉及代码】
你截图问的产品描述怎么填，我在上一条消息里已经给了具体文案和
用词建议（避免"塔罗""占卜""算命"这类词，改用"自我探索""性格
洞察"这种更中性的描述），这里不重复贴一遍，往上翻一条就是。
